# frozen_string_literal: true

#
# Generated by serializers-code-gen
# Descriptor: ConfigRootKeyOnSerializer
# Mode: :json
# Config: null_for_missing_has_one=true, supports_root_key=true, hash_record_key_type=:string, hash_output_key_type=:string, json_column_emit=:wire_format, pool_writer=true
#
# To use:
#   require_relative "config_root_key_on_serializer_json.generated"
#   ConfigRootKeyOnSerializer_JSON.new(descriptor: descriptor).serialize_one(record)

class ConfigRootKeyOnSerializer_JSON
  FIELD_INDEX = {id: 0}.freeze
  POOL = Panko::CodeGen::WritersPool::IsolatedExecutionState.new(:_scg_writer__ConfigRootKeyOnSerializer_JSON)

  def initialize(descriptor:)
  end

  def serialize_one(record, context: nil, scope: nil, filters: nil, root_key: nil)
    filters = Panko::CodeGen::Filter.wrap(filters, FIELD_INDEX)
    validate_root_key!(root_key)
    writer = POOL.checkout
    begin
      if root_key
        writer.push_object
        writer.push_key(root_key)
      end
      _write_one(record, writer, context, scope, filters)
      writer.pop if root_key
      result = writer.to_s
      result.chomp!
      result
    ensure
      POOL.checkin(writer)
    end
  end

  def serialize_many(records, context: nil, scope: nil, filters: nil, root_key: nil)
    filters = Panko::CodeGen::Filter.wrap(filters, FIELD_INDEX)
    validate_root_key!(root_key)
    writer = POOL.checkout
    begin
      writer.push_object if root_key
      writer.push_array(root_key)
      records.each { |r| _write_one(r, writer, context, scope, filters) }
      writer.pop
      writer.pop if root_key
      result = writer.to_s
      result.chomp!
      result
    ensure
      POOL.checkin(writer)
    end
  end

  def _release
    nil
  end

  def _write_one(record, writer, context, scope, filters)
    if record.is_a?(Hash)
      writer.push_object
      unless filters.drops?(0)
        writer.push_value(record["id"], "id")
      end
      writer.pop
    else
      writer.push_object
      unless filters.drops?(0)
        writer.push_value(record.id, "id")
      end
      writer.pop
    end
  end

  private def validate_root_key!(root_key)
    return if root_key.nil? || (root_key.is_a?(String) && !root_key.empty?)
    raise ArgumentError, "root_key: must be a non-empty String, got #{root_key.inspect}"
  end
end
