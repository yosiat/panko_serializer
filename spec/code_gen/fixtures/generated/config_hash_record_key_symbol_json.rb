# frozen_string_literal: true

#
# Generated by serializers-code-gen
# Descriptor: ConfigHashRecordKeySymbolSerializer
# Mode: :json
# Config: null_for_missing_has_one=true, supports_root_key=false, hash_record_key_type=:symbol, hash_output_key_type=:string, json_column_emit=:wire_format, pool_writer=true
#
# To use:
#   require_relative "config_hash_record_key_symbol_serializer_json.generated"
#   ConfigHashRecordKeySymbolSerializer_JSON.new(descriptor: descriptor).serialize_one(record)

class ConfigHashRecordKeySymbolSerializer_JSON
  FIELD_INDEX = {id: 0, name: 1}.freeze
  POOL = SerializersCodeGen::WritersPool::IsolatedExecutionState.new(:_scg_writer__ConfigHashRecordKeySymbolSerializer_JSON)

  def initialize(descriptor:)
  end

  def serialize_one(record, context: nil, scope: nil, filters: nil)
    filters = SerializersCodeGen::Filter.wrap(filters, FIELD_INDEX)
    writer = POOL.checkout
    begin
      _write_one(record, writer, context, scope, filters)
      result = writer.to_s
      result.chomp!
      result
    ensure
      POOL.checkin(writer)
    end
  end

  def serialize_many(records, context: nil, scope: nil, filters: nil)
    filters = SerializersCodeGen::Filter.wrap(filters, FIELD_INDEX)
    writer = POOL.checkout
    begin
      writer.push_array
      records.each { |r| _write_one(r, writer, context, scope, filters) }
      writer.pop
      result = writer.to_s
      result.chomp!
      result
    ensure
      POOL.checkin(writer)
    end
  end

  def _write_one(record, writer, context, scope, filters)
    if record.is_a?(Hash)
      _write_one_hash(record, writer, context, scope, filters)
    else
      _write_one_object(record, writer, context, scope, filters)
    end
  end

  def _write_one_hash(record, writer, context, scope, filters)
    writer.push_object
    unless filters.drops?(0)
      writer.push_value(record[:id], "id")
    end
    unless filters.drops?(1)
      writer.push_value(record[:name], "name")
    end
    writer.pop
  end

  def _write_one_object(record, writer, context, scope, filters)
    writer.push_object
    unless filters.drops?(0)
      writer.push_value(record.id, "id")
    end
    unless filters.drops?(1)
      writer.push_value(record.name, "name")
    end
    writer.pop
  end
end
